# Books

Put your own PDF copies here:

- `system-design-interview-vol-1.pdf`
- `system-design-interview-vol-2.pdf`

They are read by `/prep/books/<slug>` and are **deliberately gitignored**. These are paid books;
the only legitimate copy is one you bought, and it should not end up in a repository. Your own
deployment is protected by Cloudflare Access, so serving it there is the same as opening the
file on your laptop.
